# APIs & Integrations

Consumed by n8n Airtable nodes (no Airtable native automations observed)

Credentials are external to the portfolio. Public docs never include tokens, webhook secrets, or `.env` values.
